{
  "buildCommand": "npm install --ignore-scripts --workspaces=false && cd app && npm install --ignore-scripts && npx tsc -p tsconfig.json --noEmit && npx vite build",
  "outputDirectory": "app/dist",
  "installCommand": "echo skip"
}
